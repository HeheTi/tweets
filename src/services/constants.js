export const FILTER_BY_FOLLOWING = {
  showAll: '',
  follow: 'isFollowing=false',
  followings: 'isFollowing=true',
};

export const LS_KEY_FILTER = 'selected-filter';

export const ENDPOINTS = {
  USERS: '/users',
};

export const LIMIT_DATA = 3;
